// router-model.mjs — canonical single source for the six ADLC phase routers (T13/T14).
//
// GENERATED SHAPE, HUMAN-OWNED CONTENT. This ES module is the ONE place the router
// content lives; scripts/router/gen-routers.mjs imports it directly and renders each
// harness file. No YAML/JSON parser and no dependency (see docs/specs/router-consolidation.md):
// content strings are plain JS string literals, so gate text containing `:`, backticks
// or `|` cannot be truncated by a block-scalar parser.
//
// TWO ROUTER CLASSES (see spec):
//   full-map  — enumerate the P0-P7 -> gate map: claude-code, antigravity (format 'prose');
//               opencode, cursor (format 'table'). include the phase map + adversarial note.
//   minimal   — short delegating routers with NO per-phase map: codex, pi (format 'minimal').
//               include the adversarial note only.
//
// Each harness renders an ordered `layout` of section ids; ids resolve to a `shared`
// block (drift-prone content defined once) or a harness-local block. `{{P3_NOTE}}` in the
// shared phase detail is substituted per-harness from `slots` (claude-code joins with
// " and "; antigravity injects its advisory-hook note). Editing a shared block updates
// every harness that includes it — that is the anti-drift guarantee.
//
// To regenerate the six files after an edit:  node scripts/router/gen-routers.mjs
// The committed files are byte-identically drift-gated against this module in CI.

export const routerModel = {
  shared: {
    "prose:intro": "\n# ADLC — phase routing\n\nThe Agentic Development Lifecycle treats agentic development as phases with\nexplicit, machine-checkable **gates**. Each gate is a CLI invoked through the\numbrella dispatcher: `adlc <tool> [args]`. Every tool exits `0` = gate passes,\n`1` = operational error, `2` = gate fails. Identify the phase, run the gate.\n\n**Claude is the model.** Every LLM-backed gate supports `--prompt-only`: it\nprints the exact prompt and exits without calling any provider. Inside Claude\nCode you do not need API keys — run the tool with `--prompt-only`, answer the\nprinted prompt yourself, and apply the judgment. Prefer this over wiring keys.\n\nPrerequisite: the toolkit must be installed (`adlc --version` works). If not,\nthe user runs `npm i -g @adlc/cli`. Run `/adlc-init` once per repo to create the\n`.adlc/` workspace.\n\n",
    "prose:where-am-i": "## Where am I? → which gate\n\n```\nVague request, no ticket yet? ───────────────→ P0  /adlc-ticket\nHave a spec / acceptance criteria? ──────────→ P1  adlc spec-lint · premortem · parallax · adversarial-review\nHave tickets, planning fan-out? ─────────────→ P2  adlc coldstart · model-router · merge-forecast\nAbout to build, want to freeze tests? ───────→ P3  adlc rails-guard · adversarial-review\nMid-build, agent looping or drifting? ───────→ P4  adlc flail-detector\nHard failing test, need a repair? ───────────→ P4  adlc consensus-fix\nChange done, pre-merge prosecution? ─────────→ P5  adlc hollow-test · behavior-diff · review-calibration · adversarial-review\nRecording / showing gate evidence? ──────────→ —   adlc gate-manifest\nRepeated review findings to bank? ───────────→ P7  /adlc-distill (lesson-foundry · rejection-mining)\nMine the repo for reusable skills? ──────────→ P7  skill-mining (npx skills add voodootikigod/skill-mining)\nIdle-time / post-drift maintenance? ─────────→ —   /adlc-maintain (skill-rot · model-ratchet · gate-fuzzing)\n```\n\n",
    "prose:adversarial-loop": "**The adversarial-review loop.** A cross-model, fresh-context ship/no-ship review that\nloops review→fix→re-review until clean (`exit 0 = SHIP`). It only reviews a git diff/branch\ntoday (positional args are focus text, not a file path) — it is recommended practice at\n**P1** (design review of the ticket/spec) and **P3** (attack the declared rail *set* for\nadequacy — is every invariant covered and unbypassable) via `--prompt-only` or diff review,\npending the deferred `--input` mode, and at **P5** (built code; ≥2 distinct providers on the\nrisk gate) it runs directly. Flags: `--verify` (refute stale findings), `--loop` (autonomous\nfix loop over working-tree code changes only, needs a write sandbox), `--providers`\n(multi-provider quorum). Installed separately — invoke via `npx adversarial-review` if not\non PATH. See ADR-0008 (adversarial-review coverage map) in the ADLC repo.\n\n",
    "prose:phases": "## The phases\n\n### P0 — Triage → `/adlc-ticket`\nTurn a request into a self-contained ticket in `.adlc/tickets.json`. Everything\ndownstream reads this file; nothing else creates it. Author here first.\n\n### P1 — Interrogate (spec is testable and stress-tested)\n- `adlc spec-lint <spec.md>` — every acceptance criterion needs a concrete\n  verification method; a \"wish\" with no method gate-fails (exit 2). Add `--llm`\n  (or `--prompt-only`) to also catch vacuous methods.\n- `adlc premortem <spec.md> [--prompt-only]` — stress-test the approved spec for\n  failure modes before implementation.\n- `adlc parallax --request \"…\"` (or `--file req.md`) — fan out readers to expose\n  ambiguity, edge conflicts, or route conflicts. The accuracy dial (D3).\n- Design review is recommended practice today via `adversarial-review --prompt-only`\n  (feed the ticket/spec to a model yourself) or `adversarial-review --base <ref>`\n  (review the diff that introduces it); `exit 0 = SHIP`. First-class artifact input\n  (`--input`) is a deferred follow-on — see ADR-0008 (adversarial-review coverage map).\n\n### P2 — Decompose (an agent can execute without guessing)\n- `adlc coldstart <ticket-id> --prompt-only` (or `--all`) — gate ticket\n  executability. LLM-backed: in Claude, use `--prompt-only` and answer the\n  printed audit yourself (the bare form needs an API key and exits 1 without one).\n- `adlc model-router [--floor <n>]` — assign tickets to frontier/direct/ladder\n  model strategies. The cost dial (D1).\n- `adlc merge-forecast` — estimate fan-out width, dependency pressure, and merge\n  backpressure. The time dial (D2).\n\n### P3 — Rail (frozen paths are protected)\n- `adlc rails-guard --base <ref> --ticket <id>` — diff-based check that no\n  committed change touched a frozen rail (exit 2 = a rail was edited). This is the\n  **unbypassable commit-time backstop**; run it in CI. The plugin's **PreToolUse\n  rail hook** is the in-session layer: it precisely denies Edit/Write/MultiEdit to\n  declared rail paths{{P3_NOTE}}freezes `.adlc/tickets.json` itself once rails exist.\n  **Bash is not gated in-session** — a shell can't be reliably parsed, so rail\n  mutations via Bash are caught by the CI diff gate (any spelling), not the hook.\n  Wire that gate with the template at `docs/ci/rails-guard.yml` and make it a\n  required check. Override deliberately with `ADLC_RAILS_BYPASS=1` (recorded to\n  the manifest).\n- Rail-set adequacy review — is every invariant covered and unbypassable — is\n  recommended practice today via `adversarial-review --prompt-only` (feed the declared\n  rail set + ticket + invariants to a model); `exit 0 = SHIP`. `--loop` reviews\n  working-tree code changes, not a not-yet-built rail set, so there is no runnable\n  `--loop` command at P3 yet. First-class artifact input (`--input`) is a deferred\n  follow-on — see ADR-0008 (adversarial-review coverage map).\n\n### P4 — Build (supervised execution)\n- `adlc flail-detector <log-file> [--scope <glob>]` — detect repeated errors,\n  scope violations, edit churn, oversized logs.\n- `adlc consensus-fix --test-cmd \"…\" --files a.mjs,b.mjs` — for a hard failing\n  test, fan out independent candidate repairs and select a gated consensus\n  winner. Exploits the generator–verifier gap (E1).\n\n### P5 — Prosecute (the change earns the merge)\n- `adlc hollow-test --test-cmd \"node --test test/\"` — mutate changed code to find\n  tests that pass without actually testing the behavior.\n- `adlc behavior-diff capture …` / `compare before.json after.json` — make\n  behavior change visible for the P6 human gate.\n- `adlc review-calibration --review-cmd \"… {base} …\"` — measure reviewer recall\n  by scoring whether review catches injected mutants (\"who reviews the reviewer\").\n- `adversarial-review --providers <a,b> [--verify]` — ≥2 distinct providers, cross-model\n  and fresh-context, on the risk gate; loop review→fix→re-review until `exit 0 = SHIP`.\n\n### P6 — Integrate (the human gate)\nThis gate is a human decision, not something an agent passes. Surface the\nevidence: `adlc gate-manifest show` and the `behavior-diff compare` output, then\nlet the human decide. Record outcomes with `adlc gate-manifest record <gate>`.\n\n### P7 — Distill (turn findings into defenses) → `/adlc-distill`\n- `adlc lesson-foundry --prompt-only` — mine repeated findings into deterministic\n  defenses (lint checks, skills). LLM-backed: answer the printed prompt yourself.\n- `adlc rejection-mining --prompt-only` — mine human PR rejections into reusable\n  review lenses (needs the `gh` CLI). `/adlc-distill` runs both.\n\nP7 has a second, complementary axis — mining the *codebase itself* (not its review\nfindings) for reusable capabilities:\n- **skill-mining** (`npx skills add voodootikigod/skill-mining`, then \"mine this\n  repo for skills\") — surveys git churn/conventions/patterns, scores candidates on\n  a five-axis rubric, dedups against installed skills + the `skills.sh` registry,\n  red-teams each authored `SKILL.md` with a fresh-context agent (Gate B), and emits\n  a `SKILLS_MINED.md` report. It is an agentic skill, not a deterministic `adlc`\n  gate (no `--prompt-only`/exit codes). Two uses: (a) standalone, to bootstrap a\n  repo's skill portfolio; (b) as the validation/registry step for `SKILL.md` stubs\n  that `/adlc-distill`'s lesson-foundry scaffolds — dedup + Gate B before they PR.\n  lesson-foundry emits stubs; skill-mining manages the registry.\n\n### Maintenance (decay-driven, no human trigger) → `/adlc-maintain`\n- `adlc skill-rot [path…]` — flag skill files with stale validation metadata.\n- `adlc model-ratchet --dry-run` — identify hot files to re-prosecute after model\n  or repo drift (a plan, not a gate).\n- `adlc gate-fuzzing --suite .adlc/gate-suite.json --prompt-only` — play the\n  adversary against the gate suite to find calibration gaps (needs a suite file).\n- `/adlc-maintain` runs these; the deterministic two also run on a cron\n  (`docs/ci/adlc-maintenance.yml`).\n\n",
    "prose:notes": "## Notes\n\n- Add `--json` to any tool for machine-readable output when orchestrating.\n- Writers default to dry-run; pass the documented `--write`/`--record`/`--append`\n  flag to actually mutate.\n- Run `adlc <tool> --help` for a tool's exact flags and exit-code specifics.\n",
    "minimal:adversarial-loop": "## The adversarial-review loop\n\nReference `adversarial-review` at **P1** (design review of the ticket/spec) and **P3**\n(attack the declared rail *set* for adequacy — is every invariant covered and\nunbypassable) — recommended practice today via `--prompt-only` or diff review, since it\nonly reviews a git diff/branch and first-class artifact input (`--input`) is a deferred\nfollow-on — and at **P5** (built code; ≥2 distinct providers on the risk gate), where it\nruns directly. It is a cross-model, fresh-context ship/no-ship review that loops\nreview→fix→re-review until clean (`exit 0 = SHIP`). Flags: `--verify` (refute stale\nfindings), `--loop` (autonomous fix loop over working-tree code changes only, needs a\nwrite sandbox), `--providers` (multi-provider quorum). Installed separately — invoke via\n`npx adversarial-review` if not on PATH. See ADR-0008 (adversarial-review coverage map)\nin the ADLC repo.\n",
    "table:head": "\n| You're trying to… | Phase | Gate |\n| --- | --- | --- |\n",
    "table:rows-rest": "| Pin down a vague spec | P1 | `adlc spec-lint`, `adlc premortem`, `adlc parallax`, `adversarial-review` (today via `--prompt-only`) |\n| Slice work across models | P2 | `adlc coldstart`, `adlc merge-forecast`, `adlc model-router` |\n| Freeze tests/contracts as rails | P3 | `adlc rails-guard` + the in-session rails-guard hook, `adversarial-review` (today via `--prompt-only`) |\n| Build under supervision | P4 | `adlc flail-detector`, `adlc consensus-fix` |\n| Prosecute before merge | P5 | `adlc hollow-test`, `adlc behavior-diff`, `adlc review-calibration`, `adversarial-review` |\n| Decide to integrate | P6 | `adlc gate-manifest` (human gate) |\n| Distill findings into defenses | P7 | `adlc lesson-foundry`, `adlc rejection-mining` |\n| Cross-model ship/no-ship loop | P1 · P3 · P5 | `adversarial-review` — fresh-context review, loop review→fix→re-review until `exit 0 = SHIP` |\n\n",
    "table:adversarial": "`adversarial-review` only reviews a git diff/branch today (no file/artifact input yet), so\nat **P1** (design review) and **P3** (attacking the declared rail *set* for adequacy — is\nevery invariant covered and unbypassable) it is recommended practice via `--prompt-only`\n(feed it the ticket/spec or rail set yourself) or by reviewing the diff that introduces\nthe change; first-class artifact input (`--input`) is a deferred follow-on. At **P5** it\nruns directly: ≥2 distinct providers on the risk gate. Flags: `--verify` (refute stale\nfindings), `--loop` (autonomous fix loop over working-tree code changes only, needs a\nwrite sandbox), `--providers` (multi-provider quorum). Installed separately — invoke via\n`npx adversarial-review` if not on PATH. See ADR-0008 (adversarial-review coverage map) in\nthe ADLC repo.\n\n",
  },
  harnesses: {
    "claude-code": {
      path: "plugins/adlc-claude-code/skills/adlc/SKILL.md",
      format: "prose",
      frontmatter: "---\nname: adlc\ndescription: Routes agentic development work to the right Agentic Development Lifecycle (ADLC) gate. Use when shaping a spec or ticket, deciding how to fan out work to models, protecting frozen rails during a build, prosecuting a change before merge, or distilling repeated review findings into defenses. Triggers on \"shape this spec\", \"is this ticket ready\", \"freeze these tests\", \"prosecute this change\", \"is this safe to merge\", \"ADLC\", \"which gate\", \"spec-lint\", \"premortem\", \"coldstart\", \"rails-guard\", \"hollow-test\", \"behavior-diff\".\n---\n",
      slots: {"P3_NOTE":" and "},
      includes: ["prose:intro","prose:where-am-i","prose:adversarial-loop","prose:phases","prose:notes"],
      local: {
        "sentinel": "<!-- ADLC_CC_SENTINEL_PHASE_ROUTER_V1 -->\n",
      },
      layout: ["sentinel","prose:intro","prose:where-am-i","prose:adversarial-loop","prose:phases","prose:notes"],
    },
    "antigravity": {
      path: "plugins/adlc-antigravity/skills/adlc/SKILL.md",
      format: "prose",
      frontmatter: "---\nname: adlc\ndescription: Routes agentic development work to the right Agentic Development Lifecycle (ADLC) gate in Antigravity. Use when shaping a spec or ticket, deciding how to fan out work to models, protecting frozen rails during a build, prosecuting a change before merge, or distilling repeated review findings into defenses. Triggers on \"shape this spec\", \"is this ticket ready\", \"freeze these tests\", \"prosecute this change\", \"is this safe to merge\", \"ADLC\", \"which gate\", \"spec-lint\", \"premortem\", \"coldstart\", \"rails-guard\", \"hollow-test\", \"behavior-diff\".\n---\n",
      slots: {"P3_NOTE":". **Antigravity note:** In this `agy` plugin the in-session\n  PreToolUse hook is **advisory** — agy fails OPEN on a non-zero hook exit, so it\n  is best-effort, not a guarantee. The unbypassable control is the CI diff gate\n  (`scripts/rails-guard-ci.mjs`). See the \"Rails in Antigravity (agy)\" section\n  below. It "},
      includes: ["prose:intro","prose:where-am-i","prose:adversarial-loop","prose:phases","prose:notes"],
      local: {
        "sentinel": "<!-- ADLC_AGY_SENTINEL_PHASE_ROUTER_V1 -->\n",
        "rails-antigravity": "\n## Rails in Antigravity (agy)\n\nThis plugin installs a `PreToolUse` rails-guard hook. It is **advisory** in-session —\nagy fails OPEN on a non-zero hook exit, so a frozen-rail write can slip through a hook\ncrash/timeout. **The unbypassable guarantee is the commit-time CI gate**\n(`scripts/rails-guard-ci.mjs`). Enforcement activates only when `ADLC_P4_ENFORCEMENT=1`\nand an active ticket declares `rails[]`. Shell (`run_command`) writes are not gated\nin-session; the CI gate catches them.\n",
      },
      layout: ["sentinel","prose:intro","prose:where-am-i","prose:adversarial-loop","prose:phases","prose:notes","rails-antigravity"],
    },
    "codex": {
      path: "plugins/adlc-codex/skills/adlc/SKILL.md",
      format: "minimal",
      frontmatter: "---\nname: adlc\ndescription: Route software work through the Agentic Development Lifecycle in Codex. Use when the user asks to apply ADLC, operate in ADLC, triage work, or choose the next ADLC gate.\n---\n",
      slots: {},
      includes: ["minimal:adversarial-loop"],
      local: {
        "body": "\nADLC_CODEX_SENTINEL_PHASE_ROUTER_V1\n\n# ADLC Router\n\nClassify the work before acting:\n\n- Trivial: direct edit, existing rails, one prosecution pass.\n- Bounded: write or identify rails, build, prosecute-lite.\n- Substantial: P1-P7 full lifecycle.\n- Architectural: P1 design alternatives, then full lifecycle.\n\nUse deterministic ADLC CLIs for pass/fail. Skills may recommend commands; they do not\ndeclare gates complete unless the relevant CLI and `.adlc/manifest.jsonl` evidence pass.\n\nStart with:\n\n```sh\nadlc preflight --json\n```\n\nFor strict phase assertions, use:\n\n```sh\nadlc run <p1|p2|p3|p4|p5|p6|p7> --dir .adlc --json\n```\n\nP3, P4, P5, and P6 assertions are ticket-scoped; include `--ticket <ticket-id>` for\nthose phases.\n\n",
      },
      layout: ["body","minimal:adversarial-loop"],
    },
    "pi": {
      path: "plugins/adlc-pi/skills/adlc/SKILL.md",
      format: "minimal",
      frontmatter: "---\nname: adlc\ndescription: Route software work through the Agentic Development Lifecycle in Pi. Use when the user asks to apply ADLC, operate in ADLC, triage work, or choose the next ADLC gate.\n---\n",
      slots: {},
      includes: ["minimal:adversarial-loop"],
      local: {
        "body": "\n# ADLC Router (Pi Integration)\n\nClassify the work before acting:\n\n- Trivial: direct edit, existing rails, one prosecution pass.\n- Bounded: write or identify rails, build, prosecute-lite.\n- Substantial: P1-P7 full lifecycle.\n- Architectural: P1 design alternatives, then full lifecycle.\n\nUse deterministic ADLC CLIs for pass/fail. Skills may recommend commands; they do not declare gates complete unless the relevant CLI and `.adlc/manifest.jsonl` evidence pass.\n\nStart with:\n\n```sh\nadlc preflight --json\n```\n\nFor strict phase assertions, use:\n\n```sh\nadlc run <p1|p2|p3|p4|p5|p6|p7> --dir .adlc --json\n```\n\nP3, P4, P5, and P6 assertions are ticket-scoped; include `--ticket <ticket-id>` for those phases.\n\n",
      },
      layout: ["body","minimal:adversarial-loop"],
    },
    "opencode": {
      path: "plugins/adlc-opencode/skill/adlc.md",
      format: "table",
      frontmatter: "---\nname: adlc\ndescription: >-\n  Routes agentic development work to the right Agentic Development Lifecycle\n  (ADLC) gate from inside OpenCode. Use when shaping a spec or ticket, deciding\n  how to fan work out to models, protecting frozen rails during a build,\n  prosecuting a change before merge, or distilling repeated findings into\n  defenses. Triggers on \"shape this spec\", \"is this ticket ready\", \"freeze these\n  tests\", \"prosecute this change\", \"is this safe to merge\", \"ADLC\", \"which gate\",\n  \"spec-lint\", \"premortem\", \"coldstart\", \"rails-guard\", \"hollow-test\".\n---\n",
      slots: {},
      includes: ["table:head","table:rows-rest","table:adversarial"],
      local: {
        "intro": "\n# ADLC phase router (OpenCode)\n\nDescribe what you're doing; this routes you to the gate that fits. Gates run via\nthe `adlc <tool>` dispatcher (`npm i -g @adlc/cli`). LLM-backed gates support\n`--prompt-only` — inside OpenCode, the model answers the printed prompt, so no\nAPI key is required.\n",
        "p0-row": "| Triage / author a ticket | P0 | `adlc preflight`, `/adlc-ticket` |\n",
        "rail-enforcement": "## Rail enforcement in this harness\n\nThe bundled plugin wires a `tool.execute.before` hook that denies structured\n`edit`/`write` to frozen rails declared by the active ticket. It is **advisory in\nsession and gated on host SDK capability** — the unbypassable layer is the\ncommit-time CI gate (`docs/ci/rails-guard.yml`). See\n[`docs/integrations/opencode.md`](../../../docs/integrations/opencode.md).\n",
      },
      layout: ["intro","table:head","p0-row","table:rows-rest","table:adversarial","rail-enforcement"],
    },
    "cursor": {
      path: "plugins/adlc-cursor/rules/adlc.mdc",
      format: "table",
      frontmatter: "---\ndescription: >-\n  Routes agentic development work to the right Agentic Development Lifecycle\n  (ADLC) gate from inside Cursor. Use when shaping a spec or ticket, deciding how\n  to fan work out to models, protecting frozen rails during a build, prosecuting a\n  change before merge, or distilling repeated findings into defenses. Triggers on\n  \"shape this spec\", \"is this ticket ready\", \"freeze these tests\", \"prosecute this\n  change\", \"is this safe to merge\", \"ADLC\", \"which gate\", \"spec-lint\", \"premortem\",\n  \"coldstart\", \"rails-guard\", \"hollow-test\".\nalwaysApply: false\n---\n",
      slots: {},
      includes: ["table:head","table:rows-rest","table:adversarial"],
      local: {
        "intro": "\n# ADLC phase router (Cursor)\n\nDescribe what you're doing; this routes you to the gate that fits. Gates run via\nthe `adlc <tool>` dispatcher (`npm i -g @adlc/cli`). LLM-backed gates support\n`--prompt-only` — inside Cursor the model answers the printed prompt, so no API\nkey is required.\n",
        "p0-row": "| Triage / author a ticket | P0 | `adlc preflight`, author into `.adlc/tickets.json` |\n",
        "rail-enforcement": "## Rail enforcement in this harness\n\nThe bundled hooks wire Cursor's `preToolUse` hook to deny structured `Write`/`Edit`\nto frozen rails declared by the active ticket, and `afterFileEdit` to surface a\npost-edit notice (it cannot block). Both are **best-effort / advisory** — Cursor's\n`permission: \"deny\"` is not guaranteed, and `afterFileEdit` is observational. The\n**unbypassable** layer is the commit-time CI gate (`docs/ci/rails-guard.yml`). See\n[`docs/integrations/cursor.md`](../../../docs/integrations/cursor.md).\n",
      },
      layout: ["intro","table:head","p0-row","table:rows-rest","table:adversarial","rail-enforcement"],
    },
  },
};

export default routerModel;
