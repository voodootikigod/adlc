{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://adlc.dev/schemas/ticket-v1.json",
  "title": "ADLC ticket",
  "description": "A stored ADLC ticket. `adlc ticket create` accepts the same document without `id`, which the store mints as a ULID.",
  "type": "object",
  "required": [
    "id",
    "title"
  ],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Ticket id. Omit it on create and the store mints a ULID (T-01K...); supply one only to keep an existing T<n> id."
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "One imperative line naming the work."
    },
    "body": {
      "description": "The self-contained ticket text: what to build, the acceptance criteria, and the concrete command that verifies each one. A fresh agent sees only this — never the conversation that produced it. coldstart audits it for gaps."
    },
    "category": {
      "description": "Routing hint, not a free-form label. model-router sends contract, spec, and architecture to a frontier model and routes the rest from empirical priors. Keep to the set ticket-sync accepts or a synced ticket cannot converge: feature, bug, bugfix, refactor, docs, chore, test, spec, contract, architecture."
    },
    "duration": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "Relative build-time estimate used to order the ticket DAG. Defaults to 1."
    },
    "budget": {
      "description": "Optional token ceiling. model-router and flail-detector honour a positive number and ignore anything else; the store does not validate it. Omit it to take the tier default."
    },
    "scope": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Path globs this ticket may touch, e.g. src/auth/**."
    },
    "rails": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Path globs frozen for the duration of the build; rails-guard denies edits to them. Once any ticket declares rails the ticket store itself becomes a frozen trust root, so later ticket writes need ADLC_RAILS_BYPASS=1."
    },
    "completed": {
      "description": "Lifecycle state, set by `adlc ticket complete` rather than authored by hand. It is part of the stored document, so an update that omits it REMOVES it — build updates from `show <id> --json`, not from scratch."
    },
    "edges": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "to"
        ],
        "properties": {
          "to": {
            "type": "string",
            "minLength": 1,
            "description": "Id of the dependent ticket, which must not start before this one completes."
          },
          "contract": {
            "description": "Path to the interface this edge guarantees the dependent ticket can consume."
          }
        },
        "additionalProperties": true
      },
      "description": "Ordering constraints, prerequisite to dependent. An edge with \"to\": \"TX\" on THIS ticket means this ticket must complete before TX — so making this ticket depend on an existing one is an edge added to that existing ticket, never a reversed edge here. An edge may also carry \"contract\": a path to the interface it guarantees TX can consume, which is what lets the two be built in parallel; ticket-sync recognizes it and nothing else on an edge."
    }
  },
  "additionalProperties": true
}
