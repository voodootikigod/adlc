# API Spec

## Acceptance Criteria

- Authentication works correctly: `run tests`
- Performance is good: verified by running the suite
- Error handling is right: test: check it works
- Pagination returns data: `curl /api/items?page=2` returns HTTP 200 with items array
