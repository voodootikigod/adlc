# Interrogation Template

Auto-generated by lesson-foundry. Answer these questions in your spec (P1).

- [ ] **[test-coverage]** Does this spec assert a STRUCTURAL property (shape, containment,
      ordering, presence) as well as the values it produces? If so, name the fixture whose
      ONLY difference is that structure — identical or absent leaf values, e.g. empty
      containers — and state that deleting the line which records the structure must fail a
      test.

      Why: twice, a suite covered the OUTCOMES of a structural check but never the
      MECHANISM. Every structural case passed incidentally through differing leaf values, so
      the single line recording container shape could be deleted with no test failing. The
      case that would have caught it — empty containers, which contribute no leaves — was
      the one never written.

      Deterministic check, not a reminder: `adlc hollow-test` must kill a mutant that
      removes the structure-recording line. If it survives, the structural assertion is
      decorative.

      *(recurring in 2 findings, cluster: a-test-suite-covered-the-outcomes-of-a-structural-)*

- [ ] **[security]** Does this spec make a security gate classify text as inert vs
      operative (fenced prose vs live marker, comment vs directive)? If so, name the
      grammar the RENDERER uses for that classification and state that the gate parses
      with that same grammar (or a conformant library) — never a regex approximation —
      and name the fixtures that pin the divergence cases: CRLF line endings on fence
      closers, and nested/mismatched fences.

      Why: twice, a hand-rolled fence classifier drifted from real CommonMark and an
      OPERATIVE suppression marker evaded scanning as "inert prose" — once via a naive
      nested-fence toggle desyncing from the grammar, once via a CRLF fence closer
      defeating a regex anchor. A scanner that disagrees with the renderer about what
      is prose is not a weaker gate; it is a bypass.

      Deterministic check, not a reminder: the suite must include CRLF and
      nested-fence corpus fixtures in which the marker IS operative, asserting the
      gate flags them. If the classifier and the renderer can be fed the same
      corpus, assert their classifications agree.

      *(recurring in 2 findings, cluster: suppression-security-gate-marked-an-operative-mdx-, cluster-id: 9cdfa3e17145a033, cluster-members: 0e1fce8347ae e3377114741b)*
