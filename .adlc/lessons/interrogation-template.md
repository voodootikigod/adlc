# Interrogation Template

Auto-generated by lesson-foundry. Answer these questions in your spec (P1).

- [ ] **[test-coverage]** Does this spec assert a STRUCTURAL property (shape, containment,
      ordering, presence) as well as the values it produces? If so, name the fixture whose
      ONLY difference is that structure — identical or absent leaf values, e.g. empty
      containers — and state that deleting the line which records the structure must fail a
      test.

      Why: twice, a suite covered the OUTCOMES of a structural check but never the
      MECHANISM. Every structural case passed incidentally through differing leaf values, so
      the single line recording container shape could be deleted with no test failing. The
      case that would have caught it — empty containers, which contribute no leaves — was
      the one never written.

      Deterministic check, not a reminder: `adlc hollow-test` must kill a mutant that
      removes the structure-recording line. If it survives, the structural assertion is
      decorative.

      *(recurring in 2 findings, cluster: a-test-suite-covered-the-outcomes-of-a-structural-)*
