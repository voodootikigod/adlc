{
  "body": "CONTEXT. Spec section 7 point 1(b) (docs/specs/segmented-gate-manifest.md): when more than one committed segment declares the current branch and no local .lineage token disambiguates them, every keyed token-less write refuses (ambiguous) — fail-closed by design, since guessing which candidate to extend silently forks the lineage. The refusal is correct; what is missing is a supported remediation. Today an operator must hand-craft the gitignored .adlc/manifest.d/.lineage token file ({segment, ulid, branch}) to choose a candidate — undocumented, easy to get wrong (the ULID must match the segment's), and repeated per checkout. This state arises legitimately: two clones of the same branch each performing their first write before either sees the other's segment, both landing. Adversarial review of the dual-mode enable ticket flagged the missing remediation twice.\n\nWORK. New `adlc gate-manifest adopt` subcommand (lib/adopt.mjs + bin wiring). Behavior: (a) dry-run by default, --write to apply, --json single-document output (repo conventions); (b) with no arguments, LIST the current branch's candidate segments (name, entry count, first/last timestamps, signed-entry count under the ambient... no — key must be an explicit parameter per the key contract: accept --key-file or reuse the bin's existing key resolution, matching how verify wires it) so the operator can choose; (c) `adopt <segment-name> --write` verifies the named segment: exists, grammar-valid name, declares the CURRENT branch in its first entry, chain intact, and — when a key is provided — authenticates like the writer's recovery gate (no tampered/unsigned-after-signed entries, at least one entry the key verifies); then writes the .lineage token {segment, ulid (from the filename), branch} atomically via the existing writeLineageToken; (d) refuses: a segment naming a DIFFERENT branch, a broken/unauthenticatable chain (with key), a repo that is not segmented, detached HEAD (no branch to bind the token to); (e) adopt never edits committed bytes — it writes only the local gitignored token, so it needs no ceremony and no rails.\n\nDESIGN CONSTRAINT (premortem, load-bearing): the .lineage token is a TRUST anchor, not just a pointer — readOwnChains treats a token match as proof this checkout itself minted the segment and skips signature verification on that basis. Adopt therefore MUST apply exactly the authentication gate resolveOpenSegment applies to a recovered candidate (chain intact under the key, AND the branch-bearing first entry carrying a verified v2 signature), and MUST honor the marker persisted auth mode (a keyed-mode forest refuses a keyless adopt). Anything less makes adopt a supported bypass of that gate from the other side. Adopt must also VERIFY THE TOKEN RESOLVES after writing it (peekOpenSegment validates the recorded ULID against the segment; a mismatch is silently ignored, so a bad write would report success while changing nothing), and list mode must enumerate candidates directly rather than calling recoverOpenSegment, which THROWS on exactly the ambiguity adopt exists to resolve.\n\nSCOPE NOTE: lineage.mjs and manifest-segments.mjs are in scope for AC5 error text and for exporting writeLineageToken (module-local today); adopt reuses it rather than duplicating the O_NOFOLLOW/symlink-replacement token writer.\n\nACCEPTANCE CRITERIA:\nAC1 — list mode: in a fixture with two same-branch committed segments and no token, `gate-manifest adopt` exits 0 and lists both candidates. Verify: CLI test spawning the bin.\nAC2 — adopt happy path: `adopt <name> --write` writes a token such that the next resolveOpenSegment takes the fast token path to the chosen segment and a subsequent append extends it; the other segment stays byte-identical. Verify: test asserting resolution and byte-identity.\nAC3 — refusals: wrong-branch segment, chain-broken segment, keyed-but-unauthenticatable segment, non-segmented repo, and detached HEAD each exit 2 and write no token. Verify: table-driven tests.\nAC4 — dry-run writes nothing; --json parses as exactly one document in every mode. Verify: tests.\nAC5 — the ambiguous-write refusal error text in both resolvers points at `gate-manifest adopt` as the remediation. Verify: grep in both lib files plus the existing ambiguity tests still passing with the updated message.\nAC6 — full suite green and mutation-gate clean: npm test; node scripts/mutation-gate.mjs origin/main --max 12.\nAC7 — trust-root prosecution per repo standard: distinct-provider cross-model approve bound to the reviewed revision; adlc prosecute --base main exits 0.\n\nPROCESS NOTES. (1) The spec edit (documenting adopt in section 7 point 1(b)) touches a frozen rail — expect the documented override path at merge. (2) Do not edit any existing ticket's body. (3) Mutation-gate rewrites prose in string literals — reword rather than pin. (4) Full suite before push.",
  "category": "feature",
  "duration": 1,
  "edges": [],
  "id": "T-01KYZDJTF7WE40JH25QTNAWB1Y",
  "rails": [],
  "scope": [
    "packages/gate-manifest/lib/adopt.mjs",
    "packages/gate-manifest/bin/gate-manifest.mjs",
    "packages/gate-manifest/test/**",
    "docs/tools/gate-manifest.md",
    "docs/specs/segmented-gate-manifest.md",
    "packages/gate-manifest/lib/lineage.mjs",
    "packages/tickets/lib/manifest-segments.mjs",
    "packages/tickets/test/**"
  ],
  "title": "gate-manifest adopt: operator command to disambiguate a branch with multiple committed segments by writing the local lineage token"
}
